Example endpoint for getting orders
Return a list of all orders in the system
---
tags:
  - Orders
parameters:
  - name: Access token
    in: header
    type: string
    description: The long hashed access token you get after logging in or signing up
    enum: 
    # required: true
    schema: 
      type: string
      required:
        - "token"
      properties: 
        token:
          type: "string"
          example: "fish and rice"

